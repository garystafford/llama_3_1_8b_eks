apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace for all resources
namespace: analysis

# Resources to include
resources:
- namespace.yaml
- model-downloader-sa.yaml
- llm-deployment.yaml

# Note: ConfigMap generation, replacements, and labels are defined in overlays
# See overlays/production/kustomization.yaml
